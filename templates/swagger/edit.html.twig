{% extends 'base.html.twig' %}

{% block body %}
    <div {{ stimulus_controller('edit', { url: path('app_yaml_show', {'id': api.id}) }) }}>
        <div id="apibar">
            <div class="apibar-title">
                <a href="{{ path('homepage') }}">Главная</a>
            </div>
            <h1>{{ api.title }}</h1>
            <div class="apibar-save">
                <button class="apibar-save-btn" {{ stimulus_action('edit', 'saveApi') }}>Сохранить</button>
            </div>
        </div>
        <div id="api-editor"></div>
    </div>
{% endblock %}